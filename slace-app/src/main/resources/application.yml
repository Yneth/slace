spring:
  application:
    name: slace
  profiles:
    active: dev
  mvc:
    static-path-pattern: /**
  session:
    store-type: redis

api.prefix: /api

jwt:
  expiration: 10080000
  rememberMeExpiration: 44640000
